<dashboard version="1.1">
  <label>Incident Investigation</label>
  
  <!-- Investigation Filters -->
  <fieldset submitButton="false">
    <input type="text" token="selected_host" searchWhenChanged="true">
      <label>Host:</label>
      <default>*</default>
    </input>
    <input type="text" token="selected_user" searchWhenChanged="true">
      <label>User:</label>
      <default>*</default>
    </input>
    <input type="text" token="selected_ip" searchWhenChanged="true">
      <label>IP Address:</label>
      <default>*</default>
    </input>
    <input type="time" token="time_range" searchWhenChanged="true">
      <label>Tijdvenster:</label>
      <default>
        <earliestTime>-24h@h</earliestTime>
        <latestTime>now</latestTime>
      </default>
    </input>
  </fieldset>
  
  <!-- Timeline -->
  <row>
    <panel>
      <title>Event Timeline for $selected_host$</title>
      <table>
        <searchString>`soc_endpoint` host=$selected_host$ | sort - _time | table _time host EventCode ProcessName CommandLine user</searchString>
        <earliest>$time_range.earliest$</earliest>
        <latest>$time_range.latest$</latest>
      </table>
    </panel>
  </row>
  
  <!-- Authentication Activity -->
  <row>
    <panel>
      <title>Authentication Activity</title>
      <table>
        <searchString>`soc_auth` (host=$selected_host$ OR user=$selected_user$) | sort - _time | table _time EventCode user host src_ip LogonType</searchString>
        <earliest>$time_range.earliest$</earliest>
        <latest>$time_range.latest$</latest>
      </table>
    </panel>
  </row>
  
  <!-- Network Activity -->
  <row>
    <panel>
      <title>Network Activity</title>
      <table>
        <searchString>`soc_network` (src_ip=$selected_ip$ OR dest_ip=$selected_ip$ OR host=$selected_host$) | sort - _time | table _time src_ip dest_ip bytes_out bytes_in protocol</searchString>
        <earliest>$time_range.earliest$</earliest>
        <latest>$time_range.latest$</latest>
      </table>
    </panel>
  </row>
  
  <!-- Threat Intel Enrichment -->
  <row>
    <panel>
      <title>Threat Intel Matches</title>
      <table>
        <searchString>`soc_network` (src_ip=$selected_ip$ OR dest_ip=$selected_ip$) | lookup threat_intel_ips_lookup ip_address AS src_ip OUTPUT threat_type AS src_threat | lookup threat_intel_ips_lookup ip_address AS dest_ip OUTPUT threat_type AS dest_threat | where isnotnull(src_threat) OR isnotnull(dest_threat) | table _time src_ip dest_ip src_threat dest_threat</searchString>
        <earliest>$time_range.earliest$</earliest>
        <latest>$time_range.latest$</latest>
      </table>
    </panel>
  </row>
</dashboard>
